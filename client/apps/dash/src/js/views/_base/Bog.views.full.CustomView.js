define(['jquery', '../../../../../../.', 'backbone'
], function ($, _, Backbone) {
    Bog.dash.views.full = {
        CustomView: Backbone.View.extend({
        })
    };
    return Bog.dash.views.full.CustomView;
});