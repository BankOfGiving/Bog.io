define(['jquery', '../../../../../../.', 'backbone'
], function ($, _, Backbone) {
    Bog.dash.views.full = {
        Display: Backbone.View.extend({
        })
    };
    return Bog.dash.views.full.Display;
});