define(['jquery', '../../../../../../.', 'backbone'
], function ($, _, Backbone) {
    Bog.dash.views.full = {
        Form: Backbone.View.extend({
        })
    };
    return Bog.dash.views.full.Form;
});