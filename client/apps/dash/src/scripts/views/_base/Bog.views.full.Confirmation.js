define([ 'jquery', 'underscore', 'backbone',
    'text!../../../templates/donations/_donation.display.html'
], function ($, _, Backbone, DetailTemplate) {
    Bog.dash.views.full = {
        Confirmation: Backbone.View.extend({


        })
    };
    return Bog.dash.views.full.Confirmation;
});
